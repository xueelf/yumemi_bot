__master:
  title: ^申请头衔[\s]?.+$

__terminal:
  sugar: ^(开启|启用|打开|关闭|禁用)[\s]?[a-z]+(18)?$
  update: ^>[\s]update[\s][a-z]+[\s][a-z0-9]+[\s][a-z0-9]+$
  quit: ^>[\s]quit$
  state: ^>[\s]state$

__tips:
  rank: ^(b|bl|国|t|tw|台|j|jp|日)服?rank$
  help: ^(帮助|文档|help)$
  login: ^(登录|login)$
  list: ^(列表|服务|list)$
  settings: ^(设置|settings)$
  ver: ^(版本|ver|version)$

gobang:
  start: ^五子棋[\s]?[0-9]*$
  move: ^落子[\s]?[a-zA-Z][1-9]+[0-9]?$
  rollback: ^悔棋$
  over: ^结束对局$

gvg:
  initGuild: ^设置(国|台|日)服公会$
  insertBattle: ^开启[\S][\S]座会战$
  insertFight: (^报刀[\s]?[0-9]+$|^尾刀$)
  deleteBattle: ^中止会战$
  updateFight: ^修改[\s][0-3](\.5)?[\s][1-9]+\d?$
  reservation: ^预约[\s]?[1-5]?$
  selectBattle: ^状态$
  saveload: ^(挂树|sl)$
  selectSaveLoad: ^查树$
  selectFight: ^查刀$
  updateBattle: ^((\u5468\u76EE|boss|\u8840\u91CF)\s?[1-9]\d*\s?){1,3}$
  acting: ^\[CQ:at,qq=\d+,text=@.*\]\s*\u4EE3\u62A5\s?\d*$
  # score: ^分数线$
  # rank: ^查询排名[\s][\S]+[\s]?[\S]*$

saucenao:
  search: ^(搜图|\[CQ:image,file=.+,url=.+\])$

setu:
  random: ^来[点张份][涩瑟色]图$
  search: ^来[点张份][\S]*[涩瑟色]图$