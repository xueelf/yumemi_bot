"use strict";
// import { readdirSync, promises } from "fs";
// import { IGroups } from "./types/yumemi";
// import { getProfile } from "./util";
// const groups: Map<number, IGroups> = new Map();
// // 写入群数据
// (async () => {
//   for (let file_name of await promises.readdir('./config/groups')) {
//     const group_id: string = file_name.split('.')[0];
//     groups.set(Number(group_id), await getProfile(group_id, './config/groups') as IGroups);
//   }
// })()
// function getGroups(): Map<number, IGroups> {
//   return groups;
// }
// export {
//   getGroups
// }
